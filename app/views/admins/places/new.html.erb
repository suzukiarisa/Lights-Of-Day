<div class="container">
  <div class="label-place-name">開催地の新規登録</div>
    <div class="place-border"></div>
      <div class="row">
		<div class="place-category-box">
		  <%= form_for @place, url: admins_places_path do |f| %>

			<%= f.text_field :name, :size => "40*40", placeholder: '開催地名を入力' %>

		    <%= f.submit '登録する', class: "btn btn-primary" %>

		  <% end %>
		</div>
	  </div>
</div>

<%= link_to "TOPへ", admins_festivals_path, class: 'btn btn-primary w-25' %>


