<%= render 'admins/sidemenu',festival: @festival_name %>

<div class="container">
  <div class="label-festival-name">フェス名の編集</div>
    <div class="festival-names-border"></div>
      <div class="row">
	    <div class="festival-name-category-box">
		  <%= form_for @festival_name, url: admins_festival_name_path do |f| %>

		  <%= f.text_field :name, autofocus: true, :size => "40*40", placeholder: '変更するフェス名を入力' %>

		  <%= f.submit '保存する', class: "btn btn-primary" %>

		  <% end %>
		</div>
	  </div>
</div>
